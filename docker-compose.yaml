version: '3'

services:
  nginx:
    image: nginx
    container_name: newara-nginx
    ports:
      - 32132:80
    # docker exec newara-nginx nginx -s reload
    volumes:
      - ./nginx:/etc/nginx/conf.d
    networks:
      - newara-network

networks:
  newara-network:
    name: newara-network
